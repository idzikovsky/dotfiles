#!/bin/sh

if [ ! -e "$BACKUP_DIR" ]; then
    mkdir -p "$BACKUP_DIR"
fi

TIMESTAMP=${TIMESTAMP:-$(date "+%Y-%m-%d-%H-%M")}

OUTFILE="${BACKUP_DIR}/backup-${TIMESTAMP}.tar.gz"
echo "Backing up $OUTFILE"

tar czf "${BACKUP_DIR}/backup-${TIMESTAMP}.tar.gz" -C "$DATA_DIR" "$WORLD_DIRNAME"
